<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>White Towns of Andalusia</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/stylesheets/editCityForm/style.css">
  </head>
  <body>
    <%- include('navbar') %>

    <main class="container-fluid">

      <section class="backg px-2 py-4">
        <div>

          <div class="row text-center py-3">
            <h3 class="col-12">Edit City</h3>
          </div>
          <form 
            action="/city/editCity/<%= result[0].city_id %>" 
            method="post"
            enctype="multipart/form-data"
            class="editForm row d-flex justify-content-evenly"
            >
            <div class="col-12 col-xl-5 mb-3">
              <label for="city_name" class="form-label">City Name</label>
              <input 
              type="text" 
              class="form-control" 
              id="city_name" 
              placeholder="City Name"
              value="<%= result[0].city_name %>"
              name="city_name"
              maxlength="70">
            </div>
            <div class="col-12 col-xl-5 mb-3">
              <label for="city_tag" class="form-label">Tag</label>
              <input 
              type="text" 
              class="form-control" 
              id="city_tag" 
              placeholder="Example: Paris, the city of love"
              value="<%= result[0].city_tag %>"
              name="city_tag"
              maxlength="100">
            </div>
            <div class="col-12 col-xl-5 mb-3">
              <label for="province" class="form-label">Province</label>
              <input 
              type="text" 
              class="form-control" 
              id="province" 
              placeholder="Province"
              value="<%= result[0].province %>"
              name="province"
              maxlength="40">
            </div>
            <div class="col-12 col-xl-5 mb-3">
              <label for="tel" class="form-label">Phone Number</label>
              <input 
              type="text" 
              class="form-control" 
              id="tel" 
              placeholder="Phone number"
              value="<%= result[0].tel %>"
              name="tel"
              maxlength="20">
            </div>
            <div class="col-12 col-xl-5 mb-3">
              <label for="description" class="form-label">Description</label>
              <textarea 
              class="form-control" 
              id="exampleFormControlTextarea1" 
              rows="3"
              name="description"
              value="<%= result[0].description %>"
              maxlength="250"></textarea>
            </div>
            <div class="col-12 col-xl-5 mb-3">
              <label for="formFile" class="form-label">Picture</label>
              <input 
              class="form-control" 
              type="file" 
              id="img"
              name="img">
            </div>
            <div class="col-12 col-xl-11 mb-3">
              <input class="col-3 col-xl-1 btn btn-primary ms-3" type="submit" value="Submit"></div>
          </form>
        </div>
      </section>





    </main>

    <%- include('footer') %>